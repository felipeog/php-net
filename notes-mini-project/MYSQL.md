# MySQL

- `docker exec -it db bash`: opens a shell inside the container
- `mysql -u root -p`: starts the MySQL interactive shell
- `show databases;`: lists databases
- `use database;`: selects database

# Composer

- `composer init`: runs interactive setup
- `composer install`: resolves and installs dependecies
- `composer update`: gets the latest version of dependencies
- `composer dump-autoload`: updates the autoloader
